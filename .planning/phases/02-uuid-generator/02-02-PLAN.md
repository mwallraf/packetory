---
phase: 02-uuid-generator
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/ui/toggle-group.tsx
  - components/ui/toggle.tsx
  - components/ui/switch.tsx
  - components/ui/input.tsx
  - components/ui/label.tsx
  - components/ui/scroll-area.tsx
  - lib/uuid/format.ts
  - lib/uuid/format.test.ts
  - app/tools/uuid/UuidTool.tsx
  - tests/e2e/uuid.spec.ts
autonomous: true
requirements: [UUID-02, UUID-03, UUID-04]
must_haves:
  truths:
    - "Switching version v4<->v7 regenerates fresh values in the new version — v7 encodes a timestamp and is structurally different from v4, so one cannot be reformatted into the other (D-01, UUID-02)."
    - "A visible Regenerate control is always present near the result, and the global Enter shortcut also triggers regeneration (D-03, UUID-03)."
    - "Changing the batch count regenerates the batch live as the count changes, with no separate Generate click (D-04, UUID-03)."
    - "Toggling case or hyphens, then toggling back, returns the byte-identical original UUID string — reformat only, never regenerate (D-02, UUID-04)."
    - "Case/hyphen formatting is a synchronous pure transform derived from the raw UUID array on each render; there is no async step to interleave or interrupt (D-02, Pitfall 4)."
    - "Batch count = 1 shows the single hero display; count 2-100 switches to the scrollable list at one deterministic branch point (count=2), not three ad-hoc treatments (D-05)."
    - "Batch count is clamped to 1-100, so a 0-row batch is unreachable and no empty-list state exists (UUID-03)."
    - "The batch list uses synchronous client-side generation with no network — no loading spinner state exists (UUID-03)."
    - "The batch list has no error state — generation cannot fail (UUID-03)."
    - "Values outside 1-100 (non-numeric, decimal, negative, >100) show the inline hint 'Enter a number between 1 and 100' with no popup/reload; the generated batch stays clamped to the last valid value (UUID-03, Copywriting Contract)."
    - "The version, case, hyphen, and export-format controls are fixed-cardinality (2-3 options) and always fully populated — no empty/loading/error/partial state is possible (UUID-02/UUID-04)."
    - statement: "A 100-row batch renders in a max-h-96 scroll container that scrolls internally without page-level layout shift/CLS"
      verification: backstop
    - statement: "Every batch row's 36/32-char UUID wraps (never clips) at 320px with a full 100-row batch"
      verification: backstop
  artifacts:
    - lib/uuid/format.ts
    - lib/uuid/format.test.ts
    - components/ui/toggle-group.tsx
    - components/ui/switch.tsx
    - components/ui/input.tsx
    - components/ui/label.tsx
    - components/ui/scroll-area.tsx
  key_links:
    - "UuidTool holds rawUuids as the single source of truth, regenerated ONLY on version switch / batch-count change / Regenerate (D-01/D-03/D-04); displayed strings are derived via lib/uuid/format.ts on every render (Pitfall 4)."
    - "useKeyboardShortcut binds `/`->focus batch-count input, Enter->regenerate, Ctrl/Cmd+C->copy hero (Phase 1 hook, first live consumer)."
  prohibitions: []
  assumptions:
    - "UUID-02 edge probe returned category=unclassified (unresolved). Flagged assumption: switching to v7 regenerates a valid v7 value (uuid.version()===7); no hidden edge. Surfaced, not dropped."
    - "UUID-03 edge probe returned category=unclassified (unresolved). Flagged assumption: a batch of N (1..100) yields N valid, structurally-correct UUIDs, with boundaries 0/1/100/101 handled by the clamp; no hidden edge. Surfaced, not dropped."
    - "CONTEXT.md's Claude's-Discretion `/` keyboard target is RESOLVED here: `/` focuses the batch-count input (the page's one free-value field), per UI-SPEC Assumption A1. Documented per CONTEXT instruction; unbound-safe via the Phase 1 hook."
---

<objective>
Turn the read-only hero into a customizable generator: version toggle (v4/v7), live batch count 1-100, an always-visible Regenerate control, case (upper/lower) and hyphen (on/off) toggles, the batch scroll list, and the live keyboard shortcuts (`/`, Enter, Ctrl/Cmd+C). Adds the framework-agnostic `lib/uuid/format.ts` reformat module.

Purpose: Deliver UUID-02, UUID-03, UUID-04 and lock the regenerate-vs-reformat distinction (D-01 regenerate on version/count/Regenerate; D-02 reformat-in-place on case/hyphen — Pitfall 4).
Output: 5 generated shadcn primitives, `lib/uuid/format.ts` (+ tests), and an extended `UuidTool.tsx`.
</objective>

## Artifacts this phase produces (created/modified in 02-02)

- `lib/uuid/format.ts` — exports `formatUuids(uuids: string[], opts: { case: "upper" | "lower"; hyphens: boolean }): string[]` (pure transform: `.toUpperCase()`/`.toLowerCase()` + optional `.replaceAll("-", "")`). Framework-agnostic.
- `lib/uuid/format.test.ts` — Vitest + fast-check round-trip/composition tests.
- `components/ui/toggle-group.tsx`, `components/ui/toggle.tsx`, `components/ui/switch.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx`, `components/ui/scroll-area.tsx` — shadcn-generated primitives.
- `app/tools/uuid/UuidTool.tsx` — extended state `{ rawUuids, version, count, case, hyphens }` (export-format field added in 02-04); controls + derived display + keyboard wiring.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uuid-generator/02-CONTEXT.md
@.planning/phases/02-uuid-generator/02-RESEARCH.md
@.planning/phases/02-uuid-generator/02-PATTERNS.md
@.planning/phases/02-uuid-generator/02-UI-SPEC.md
@.planning/phases/02-uuid-generator/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/uuid/format.ts — case + hyphen reformat (TDD, reformat NOT regenerate)</name>
  <files>lib/uuid/format.ts, lib/uuid/format.test.ts</files>
  <read_first>
    - lib/network/parseForwardedIp.ts (framework-agnostic module shape)
    - lib/uuid/generate.ts (existing sibling module conventions from 02-01)
    - lib/network/parseForwardedIp.test.ts (Vitest shape) and lib/uuid/generate.test.ts (fast-check usage from 02-01)
    - .planning/phases/02-uuid-generator/02-RESEARCH.md (Pitfall 4 — reformat vs regenerate; format.ts intent)
    - .planning/phases/02-uuid-generator/02-CONTEXT.md (D-02)
  </read_first>
  <behavior>
    - formatUuids(x, {case:"lower", hyphens:true}) returns canonical lowercase hyphenated form unchanged.
    - {case:"upper"} uppercases hex; {hyphens:false} removes all hyphens (32 chars).
    - Round-trip identity (fast-check property): formatUuids(formatUuids(x, A), B) where B inverts A returns the byte-identical original array — case toggle twice and hyphen toggle twice each restore the original (Pitfall 4's concrete assertion).
    - Order-independence: applying case then hyphen equals hyphen then case (the two transforms commute).
    - Total: never throws for a valid input array; empty array in -> empty array out.
  </behavior>
  <action>
    Create `lib/uuid/format.ts` with NO React/Next import. Export `formatUuids(uuids, opts)`. Implement as a pure map: per string, apply `opts.case === "upper" ? s.toUpperCase() : s.toLowerCase()`, then `opts.hyphens ? s : s.replaceAll("-", "")`. Doc-comment it against D-02 ("reformat the values already on screen in place — no new randomness, no change to underlying identity"); make explicit that this module NEVER calls a generator.
    Create `lib/uuid/format.test.ts` (Vitest + @fast-check/vitest). Cover the behaviors above; use an fast-check arbitrary that generates valid UUIDs (e.g. map over `generateBatch`) to drive the round-trip property. Title each test citing D-02/UUID-04.
  </action>
  <verify>
    <automated>npx vitest run lib/uuid/format.test.ts</automated>
  </verify>
  <done>format.ts exports a pure formatUuids that reformats without regenerating; format.test.ts passes including the double-toggle byte-identical round-trip property and the commutativity property.</done>
  <acceptance_criteria>
    - `lib/uuid/format.ts` contains no `import` from "react"/"next" and no `import` from "uuid" (it must not generate).
    - A test asserts `formatUuids(formatUuids(x,{case:"upper",hyphens:true}),{case:"lower",hyphens:true})` deep-equals the original lowercase input.
    - `npx vitest run lib/uuid/format.test.ts` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generate shadcn primitives + wire version/batch/regenerate/case/hyphen controls + keyboard shortcuts</name>
  <files>components/ui/toggle-group.tsx, components/ui/toggle.tsx, components/ui/switch.tsx, components/ui/input.tsx, components/ui/label.tsx, components/ui/scroll-area.tsx, app/tools/uuid/UuidTool.tsx</files>
  <read_first>
    - app/tools/uuid/UuidTool.tsx (current hero-only island from 02-01 — extend it, do not rewrite the copy pattern)
    - components/ui/tooltip.tsx and components/ui/button.tsx (how existing shadcn primitives import from the unified "radix-ui" package)
    - lib/hooks/useKeyboardShortcut.ts (handlers map: slash/enter/escape/copy; editable-field guard is already handled — do NOT reimplement it)
    - lib/uuid/generate.ts (generateBatch signature) and lib/uuid/format.ts (formatUuids signature)
    - .planning/phases/02-uuid-generator/02-UI-SPEC.md (Color reserved list; Copywriting Contract batch-count error + regenerate; `/` target rationale; toggle-group for version, switch for case/hyphens, input for count, scroll-area max-h-96 for the list)
    - .planning/phases/02-uuid-generator/02-PATTERNS.md (state-shape discipline: one typed state object; rawUuids regenerated only on version/count/Regenerate; display derived via format.ts)
  </read_first>
  <action>
    Generate the primitives: run `npx shadcn@latest add toggle-group switch input label scroll-area` (writes toggle-group + its internal toggle, switch, input, label, scroll-area into components/ui/ against the existing radix-nova style; per RESEARCH these resolve to the already-installed radix-ui, adding no npm dependency). Do not hand-author or hand-edit these generated files.
    Extend `UuidTool.tsx` state to `{ rawUuids: string[]; version: "v4" | "v7"; count: number; case: "upper" | "lower"; hyphens: boolean }` with defaults version "v4", count 1, case "lower", hyphens true. Establish the single-source-of-truth rule (Pitfall 4): `rawUuids` is regenerated via `generateBatch({version, count})` ONLY on (a) version switch (D-01), (b) batch-count change (D-04), (c) Regenerate click / Enter (D-03). The DISPLAYED strings are always `formatUuids(rawUuids, {case, hyphens})` computed on render — case/hyphen toggles must NOT regenerate (D-02).
    Controls: a `ToggleGroup` (single-select) for version v4/v7 with accent on the selected option only; a numeric `Input` (with a `Label` "Batch count") bound to `count`, clamped to integer 1-100 on change (reuse generate.ts's clamp semantics) — when the raw typed value is outside 1-100 or non-numeric/decimal/negative, show the inline hint "Enter a number between 1 and 100" below the input while keeping the batch at the last valid value; a Regenerate icon button (`RefreshCw`, `data-testid="uuid-regenerate"`, accent-tinted) always visible near the result; two `Switch`es labelled "Case" (upper/lower) and "Hyphens" (on/off) using the switch's own track styling (not page accent).
    Display branch (D-05): count===1 -> the existing single hero display; count>=2 -> a `ScrollArea` (`max-h-96`, `data-testid="uuid-batch-list"`) rendering one derived UUID per row in Body-size Geist Mono with `break-all`. The hero copy button/status from 02-01 stays for count===1.
    Keyboard wiring (first live consumer of the Phase 1 hook): call `useKeyboardShortcut({ slash: focus the batch-count input via a ref, enter: regenerate, copy: copy the hero/primary value })`. This resolves CONTEXT.md's flagged `/` discretion to "focus batch-count input" (UI-SPEC A1). Do not rebind Escape this phase.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run lib/uuid</automated>
  </verify>
  <done>Version toggle regenerates fresh v4/v7 values; batch count 1-100 regenerates live and shows an inline out-of-range hint without blocking; Regenerate button and Enter both produce new values; case/hyphen switches reformat in place without new randomness; count>=2 renders the scroll list; `/` focuses the batch-count input, Ctrl/Cmd+C copies the hero.</done>
  <acceptance_criteria>
    - `components/ui/toggle-group.tsx`, `switch.tsx`, `input.tsx`, `label.tsx`, `scroll-area.tsx` all exist and import from "radix-ui".
    - UuidTool.tsx computes the displayed values via `formatUuids(...)` (case/hyphen never call generateBatch).
    - `grep -q "max-h-96" app/tools/uuid/UuidTool.tsx` (batch scroll container present).
    - `grep -q "useKeyboardShortcut" app/tools/uuid/UuidTool.tsx` with a `slash` handler focusing the count input.
    - `npm run typecheck` and `npm run lint` exit 0; `npx vitest run lib/uuid` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend e2e spec — version switch, batch, regenerate, reformat, keyboard</name>
  <files>tests/e2e/uuid.spec.ts</files>
  <read_first>
    - tests/e2e/uuid.spec.ts (the spec from 02-01 — add to it, keep the existing tests)
    - tests/e2e/ip-widget.spec.ts (320px viewport backstop pattern via setViewportSize)
    - app/tools/uuid/UuidTool.tsx (data-testid names: uuid-hero-value, uuid-regenerate, uuid-batch-list, batch-count input)
    - .planning/phases/02-uuid-generator/02-VALIDATION.md (Test Map for UUID-02/03/04; Manual-Only visual backstops)
  </read_first>
  <action>
    Extend `tests/e2e/uuid.spec.ts` with tests keyed to VALIDATION.md: "switches to v7" (select v7 in the version toggle, assert the hero value's 13th nibble is "7" per the UUID version position, and that it changed from the prior value); "generates a batch of N" (set batch count to 5, assert `uuid-batch-list` has 5 rows; set to 100, assert 100 rows and that the list scrolls within its container without page horizontal scroll); "regenerate produces a new value" (capture hero, click `uuid-regenerate`, assert value changed); "case/hyphen toggles reformat without regenerating" (capture the hero value, toggle hyphens off then on, assert the value returns byte-identical — the reformat-not-regenerate guarantee at the UI level); "`/` focuses the batch-count input" (press "/", assert the count input is focused). Give the batch/hero tests a `page.setViewportSize({width:320,height:700})` variant asserting no horizontal scroll (the long-text + overflow visual backstops).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/uuid.spec.ts</automated>
  </verify>
  <done>All new e2e tests for version switch, batch sizing, regenerate, reformat round-trip, and keyboard focus pass; the 320px backstop tests confirm no horizontal scroll for the hero and a 100-row batch.</done>
  <acceptance_criteria>
    - `tests/e2e/uuid.spec.ts` contains tests matching `-g "v7"`, `-g "batch"`, `-g "regenerate"`, and `-g "reformat"`.
    - `npx playwright test tests/e2e/uuid.spec.ts` passes (all tests, including the ones added in 02-01).
    - A test asserts toggling hyphens off then on returns the byte-identical hero value (D-02 UI-level).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> batch-count Input | The one free-value field on the page; a user (or a crafted URL/paste) can enter arbitrary text. |
| (none network) | Regeneration is fully in-browser; no server round-trip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Denial of Service | batch-count Input in UuidTool.tsx | medium | mitigate | Clamp the parsed batch count to an integer in [1,100] before it is used as an array length (both in the UI and defensively in generate.ts); reject non-numeric/decimal/negative/>100 with the inline hint and keep the last valid value. Prevents pathological array allocation from a huge/negative count (ASVS V5). |
| T-02-03 | Tampering | regenerate / version-switch RNG | low | mitigate | All regeneration routes through `uuid`'s v4()/v7() (CSPRNG); never substitute a non-cryptographic RNG. |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test && npm run test:e2e` all green.
- Manual (VALIDATION Manual-Only): at 320px, the hero value wraps (hyphens on and off) and a 100-row batch scrolls inside max-h-96 with no page CLS.
</verification>

<success_criteria>
Users can switch v4/v7, regenerate (button or Enter), set a live batch of 1-100, and toggle case/hyphens — with case/hyphen changes reformatting in place (byte-identical round-trip) and never regenerating. `/` focuses the batch-count input.
</success_criteria>

<output>
Create `.planning/phases/02-uuid-generator/02-02-SUMMARY.md` when done.
</output>
