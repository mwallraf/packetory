---
phase: 03-ip-subnet-calculator
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - lib/subnet/ipv6.ts
  - lib/subnet/ipv6.test.ts
  - app/tools/subnet/SubnetTool.tsx
  - app/tools/subnet/SubnetTool.test.tsx
  - tests/e2e/subnet.spec.ts
autonomous: true
requirements: [SUBNET-01, SUBNET-05, SUBNET-06]
must_haves:
  truths:
    - "computeIpv6 returns the normalized prefix, RFC-5952 compressed notation, expanded notation, first and last address, and total address count — all derived via 128-bit BigInt math (SUBNET-05)."
    - "IPv6 address-count branches explicitly on 128 minus prefix: a /127 shows both addresses usable with count 2 and a note (same no-broadcast convention as IPv4 /31), and a /128 shows a single address with count 1 and first equal to last (SUBNET-05 boundary edge, D-04)."
    - "All 128-bit IPv6 math uses BigInt exclusively with no Number() coercion of any address value; a fast-check property test asserts the address count equals the exact 2^(128-prefix) formula (with boundary adjustment) across every prefix 0-128 (SUBNET-05 precision edge)."
    - "parseCidr auto-detects an IPv6 CIDR by its colons and SubnetTool renders the IPv6 field grid (normalized prefix, compressed, expanded, first, last, address count, reverse-DNS) for it — exactly one of the IPv4/IPv6 grids shows per calculation (SUBNET-01 detection, SUBNET-05, UI-SPEC zero-one-many)."
    - "Every IPv6 field renders with its own copy button and independent confirmation state; boundary cases render real values plus a note, never N/A (SUBNET-06)."
    - statement: "At 320px, the 39-char expanded IPv6 notation and the IPv6 reverse-DNS zone wrap and never clip or trigger horizontal page scroll — verified by a 320px visual/e2e check for the IPv6 family."
      verification: backstop
  artifacts:
    - "lib/subnet/ipv6.ts exporting computeIpv6() and type Ipv6Result"
    - "app/tools/subnet/SubnetTool.tsx extended with the IPv6 field grid (compressed/expanded/first/last/address count/reverse-DNS)"
    - "tests/e2e/subnet.spec.ts extended with an IPv6 default/lookup case"
  key_links:
    - "computeIpv6 consumes parseCidr's ipv6 ParsedCidr; SubnetTool renders its result via compressIpv6/expandIpv6 (from format.ts) and ipv6ReverseZone (from reverse-dns.ts), both built in 03-02."
    - "SubnetTool chooses the IPv4 vs IPv6 grid from the parsed family — the same URL-state + per-field-copy plumbing built in 03-01, no new URL mechanism."
---

<objective>
Add the IPv6 family as a complete vertical slice: the `lib/subnet/ipv6.ts` 128-bit BigInt math module plus the IPv6 field grid in SubnetTool, so entering an IPv6 CIDR yields the normalized prefix, RFC-5952 compressed + expanded notation, first/last address, address count, and `ip6.arpa` reverse-DNS zone — every field copyable, correct at the /127 and /128 boundaries.

Purpose: IPv6 is the phase's second address family and the reason BigInt (not Number) is mandatory (D-03) — 128-bit math cannot use JS Number. This plan reuses the RFC-5952 and `ip6.arpa` formatters built in 03-02 and the URL-state/copy plumbing built in 03-01, so it stays focused on the math and the grid.

Output: `lib/subnet/ipv6.ts` (+ property tests over 0-128), the IPv6 grid in SubnetTool, and an IPv6 e2e case.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ip-subnet-calculator/03-RESEARCH.md
@.planning/phases/03-ip-subnet-calculator/03-PATTERNS.md
@.planning/phases/03-ip-subnet-calculator/03-UI-SPEC.md
@lib/subnet/parse.ts
@lib/subnet/ipv4.ts
@lib/subnet/format.ts
@lib/subnet/reverse-dns.ts
@app/tools/subnet/SubnetTool.tsx
</context>

## Phase Goal (MVP user story)

**As a** network engineer, **I want to** enter an IPv4 or IPv6 CIDR and instantly see its full, mathematically-correct subnet breakdown with every field copyable and the result reflected in a shareable URL, **so that** I can get my subnet answer in seconds and hand a teammate a link that reproduces it exactly.

## MVP Slice — what a user can do after this plan

Before: entering an IPv6 CIDR auto-detects the family but has no grid to render. After: a user enters (or bookmarks) an IPv6 CIDR like `2001:db8::/32` and sees its normalized prefix, RFC-5952 compressed + expanded forms, first/last address, address count, and `ip6.arpa` reverse-DNS zone — each copyable, correct at `/127` and `/128`. (Interactive /48–/64 subdivision pills arrive in 03-04.)

## Artifacts this phase produces (this plan)

| Symbol / File | Kind |
|---------------|------|
| `lib/subnet/ipv6.ts` | new file |
| `computeIpv6(parsed: ParsedCidr): Ipv6Result` | function |
| `Ipv6Result` (normalizedPrefix, compressed, expanded, firstAddress, lastAddress, addressCount, prefixLength, boundaryNote) | type |
| `app/tools/subnet/SubnetTool.tsx` | modified (IPv6 grid) |
| `tests/e2e/subnet.spec.ts` | modified (IPv6 case) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffolds (RED) for ipv6 math + IPv6 e2e</name>
  <files>lib/subnet/ipv6.test.ts, tests/e2e/subnet.spec.ts</files>
  <read_first>
    - lib/subnet/ipv4.test.ts (sibling math-module test shape + fast-check property test to mirror over 0-128)
    - lib/uuid/generate.test.ts (fcIt.prop arbitrary usage)
    - .planning/phases/03-ip-subnet-calculator/03-RESEARCH.md (Pitfall 3 boundary branching /127//128; Pattern 3 BigInt mask math; State of the Art /64 building block)
    - tests/e2e/subnet.spec.ts (existing IPv4 e2e conventions to extend)
  </read_first>
  <behavior>
    - ipv6.test.ts: 2001:db8::/32 -> compressed "2001:db8::", expanded full 8-group form, first address 2001:db8:: , last address 2001:db8:ffff:ffff:ffff:ffff:ffff:ffff, addressCount "79228162514264337593543950336" (2^96); 2001:db8::1/127 -> both addresses usable, addressCount "2", boundaryNote non-null; 2001:db8::5/128 -> first==last, addressCount "1", boundaryNote non-null; property test over prefix 0-128 asserting addressCount equals the exact BigInt 2^(128-prefix) formula with the /127 and /128 boundary adjustment.
    - subnet.spec.ts: loading /tools/subnet?cidr=2001%3Adb8%3A%3A%2F32 renders the IPv6 grid (compressed value 2001:db8:: visible) and hides the IPv4-only rows.
  </behavior>
  <action>Author lib/subnet/ipv6.test.ts against the not-yet-existing `@/lib/subnet/ipv6` module (RED), mirroring ipv4.test.ts and adding an `fcIt.prop([fc.integer({ min: 0, max: 128 })])` property test asserting the exact 2^(128-prefix) address-count formula with boundary adjustment. Extend tests/e2e/subnet.spec.ts with an IPv6 case that loads an `?cidr=` IPv6 URL and asserts the IPv6 grid renders. Do not implement ipv6.ts in this task.</action>
  <verify>
    <automated>npx vitest run lib/subnet/ipv6.test.ts 2>&1 | grep -Eq "fail|error|Cannot find module|No test files" && echo "RED-as-expected"</automated>
  </verify>
  <acceptance_criteria>
    - lib/subnet/ipv6.test.ts exists and fails (RED); the e2e IPv6 case is added.
    - `grep -c "fcIt.prop\|it.prop\|test.prop" lib/subnet/ipv6.test.ts` is >= 1 with an integer arbitrary spanning 0-128.
    - The test asserts BOTH the /127 (count 2) and /128 (count 1) boundaries.
  </acceptance_criteria>
  <done>A RED IPv6 math test (with a 0-128 property test) and an IPv6 e2e case exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/subnet/ipv6.ts (128-bit BigInt math, GREEN)</name>
  <files>lib/subnet/ipv6.ts</files>
  <read_first>
    - lib/subnet/ipv4.ts (sibling module contract: Result shape, BigInt mask helpers, boundary branching)
    - lib/subnet/format.ts (compressIpv6/expandIpv6 to reuse for the compressed/expanded fields)
    - .planning/phases/03-ip-subnet-calculator/03-RESEARCH.md (Pattern 3 maskFor/networkAddress over 128-bit; Pitfall 3 /127//128; Pitfall 4 BigInt-only)
    - lib/subnet/ipv6.test.ts (contract to satisfy)
  </read_first>
  <behavior>
    - computeIpv6 computes the network (first) address via a 128-bit prefix mask, the last address via OR with the inverted mask, and addressCount branching on 128-prefix: >= 2 uses 2^hostBits, == 1 (/127) both usable count 2 with note, == 0 (/128) single address count 1 with note. compressed and expanded are produced by reusing format.ts. normalizedPrefix is the canonical compressed network address plus /prefix. addressCount is a decimal string.
  </behavior>
  <action>Implement lib/subnet/ipv6.ts `computeIpv6(parsed)` returning `Ipv6Result` using 128-bit BigInt mask arithmetic (bitWidth 128n) per RESEARCH Pattern 3, reusing `compressIpv6`/`expandIpv6` from `@/lib/subnet/format` for the compressed/expanded and normalized-prefix fields. Branch addressCount on `128 - prefixLength` per RESEARCH Pitfall 3 (note strings for /127 and /128, else null). Return firstAddress/lastAddress in RFC-5952 compressed form and addressCount as a decimal string. Use only BigInt operators end-to-end; never coerce an address value to a JS number. Cite D-03/D-04 and RFC 5952 in the doc-comment; no React/Next import.</action>
  <verify>
    <automated>npx vitest run lib/subnet/ipv6.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/subnet/ipv6.test.ts` passes including the 0-128 property test and the /127 + /128 boundaries.
    - `grep -c "Number(" lib/subnet/ipv6.ts` is 0.
    - `grep -Eqc "React|next/" lib/subnet/ipv6.ts` finds no framework import.
  </acceptance_criteria>
  <done>ipv6.ts is a green pure 128-bit BigInt module; SUBNET-05 field math and the /127//128 boundaries (D-04) are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the IPv6 field grid in SubnetTool (GREEN)</name>
  <files>app/tools/subnet/SubnetTool.tsx, app/tools/subnet/SubnetTool.test.tsx</files>
  <read_first>
    - app/tools/subnet/SubnetTool.tsx (family-detect + IPv4 grid + CopyableField built in 03-01/03-02)
    - lib/subnet/ipv6.ts (Ipv6Result shape)
    - lib/subnet/reverse-dns.ts (ipv6ReverseZone for the IPv6 reverse-DNS row)
    - .planning/phases/03-ip-subnet-calculator/03-UI-SPEC.md (IPv6 grid = 6 fields; family badge; Separator before subdivision section; boundary note copywriting)
    - app/tools/subnet/SubnetTool.test.tsx (existing component-test conventions)
  </read_first>
  <behavior>
    - When parseCidr returns family ipv6, SubnetTool renders the IPv6 grid (normalized prefix, compressed, expanded, first address, last address, address count, reverse-DNS) instead of the IPv4 grid — never both; the family Badge shows "IPv6".
    - Each IPv6 field has its own copy button; /127 and /128 render their boundary notes; the reverse-DNS row uses ipv6ReverseZone with the same aligned/non-aligned note treatment as IPv4.
    - Extend SubnetTool.test.tsx: an IPv6 CIDR renders the IPv6 grid and hides IPv4-only fields; a /128 renders the single-address note; each field is copyable.
  </behavior>
  <action>Extend SubnetTool.tsx: import `computeIpv6` from `@/lib/subnet/ipv6` and `ipv6ReverseZone` from `@/lib/subnet/reverse-dns`; branch the rendered grid on the parsed family so an ipv6 result renders the 6-field IPv6 grid plus its reverse-DNS row (using the same CopyableField + boundary-note + aligned-note components already built), and an ipv4 result renders the IPv4 grid as before. Show the family Badge text from the parsed family. Add `subnet-field-<name>` / `subnet-copy-<name>` testids for the IPv6 fields (compressed, expanded, first, last, address-count, normalized-prefix, reverse-dns). Add a `Separator` before the (still-empty this plan) subdivision section slot per the UI-SPEC page structure. Extend SubnetTool.test.tsx with the IPv6-grid, /128-note, and copy-button assertions.</action>
  <verify>
    <automated>npx vitest run app/tools/subnet && npx playwright test tests/e2e/subnet.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/tools/subnet` and `npx playwright test tests/e2e/subnet.spec.ts` pass, including the IPv6 grid render and /128 boundary note.
    - `grep -c "computeIpv6" app/tools/subnet/SubnetTool.tsx` is >= 1.
    - Exactly one family grid renders per calculation (an IPv6 CIDR hides IPv4-only rows and vice versa), asserted in the component test.
  </acceptance_criteria>
  <done>Entering or bookmarking an IPv6 CIDR renders the full IPv6 breakdown with every field copyable and correct /127//128 boundaries. SUBNET-05 field output is satisfied (subdivisions follow in 03-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPv6 `?cidr=` value -> parse -> 128-bit math -> render | User-controlled IPv6 CIDR flows through the pure BigInt parser and formatters into React text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / EoP | IPv6 values rendered into SubnetTool | high | mitigate | Rendered only as React text content (default JSX escaping); no `dangerouslySetInnerHTML` for user input. |
| T-03-02 | Denial of Service | IPv6 literal validation | medium | mitigate | IPv6 syntax validated by the bounded WHATWG URL bracket-wrap in parseCidr (03-01); no backtracking regex; 128-bit math is O(1) BigInt ops. |
| T-03-06 | Tampering (precision) | 128-bit address/count math | medium | mitigate | BigInt end-to-end (D-03); no Number() coercion — a property test over 0-128 proves counts are exact, never wrapped/rounded. |
| T-03-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase (D-03). Nothing to verify. |
</threat_model>

<verification>
- `npx vitest run lib/subnet/ipv6.test.ts` (incl. 0-128 property test) and `npx vitest run app/tools/subnet` green.
- `npx playwright test tests/e2e/subnet.spec.ts` green (IPv4 + IPv6 cases).
- `npm run lint && npx tsc --noEmit` clean.
</verification>

<success_criteria>
A user enters or bookmarks an IPv6 CIDR and sees the full RFC-5952-correct IPv6 breakdown (normalized prefix, compressed, expanded, first/last, address count, reverse-DNS), each field copyable, correct at /127 and /128. SUBNET-01 (IPv6 detection) and SUBNET-05 field output are satisfied and test-proven.
</success_criteria>

<output>
Create `.planning/phases/03-ip-subnet-calculator/03-03-SUMMARY.md` when done.
</output>
