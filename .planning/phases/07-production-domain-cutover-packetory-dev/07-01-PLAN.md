---
phase: 07-production-domain-cutover-packetory-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOM-01, DOM-02, DOM-03, DOM-04]
tags: [domain, seo, canonical-url, docs, descope]

must_haves:
  truths:
    - "Repo-wide grep for the legacy Vercel preview origin literal across app/, vercel.json, and next.config.ts returns zero matches — DOM-03 confirmed, with the grep output captured as evidence (per D-08)"
    - "SITE_URL in app/sitemap.ts is https://packetory.dev and every canonical URL, OpenGraph url, sitemap entry, and robots sitemap: field derives from that single constant — no independently hardcoded origin exists (per D-07)"
    - "REQUIREMENTS.md marks DOM-01 and DOM-03 complete and DOM-02 and DOM-04 descoped (never left as Pending), with the Traceability table rows updated to match (per D-04/D-06)"
    - "ROADMAP.md Phase 7 records DOM-01 as verified-live (HTTP/2 200 + valid TLS 2026-07-25) and marks success criteria 2, 3, and 5 as descoped with the user-decision rationale preserved (per D-01/D-04/D-06)"
    - "PROJECT.md v1.1 progress reflects DOM-01 verified-live and DOM-02/DOM-04 deliberately descoped, preserving the accepted duplicate-content and www-TLS-failure consequences for future readers (per D-04)"
    - { statement: "next.config.ts and vercel.json contain NO host-based redirect rule — the redirect work is deliberately not implemented this phase (D-03)", verification: backstop }
    - { statement: "No runbook document is created and DEPLOY.md is not extended — the runbook is deliberately not written this phase (D-05)", verification: backstop }
  artifacts:
    - ".planning/REQUIREMENTS.md (modified — DOM-01/03 complete, DOM-02/04 descoped, traceability updated)"
    - ".planning/ROADMAP.md (modified — Phase 7 criteria 2/3/5 descoped, DOM-01 verified-live)"
    - ".planning/PROJECT.md (modified — v1.1 progress note reconciled)"
  key_links:
    - "SITE_URL (app/sitemap.ts:9) is the single origin source of truth all references derive from — the DOM-03 audit confirms nothing bypasses it"
    - "The DOM-03 grep evidence is the sole proof backing the DOM-03-complete claim recorded in REQUIREMENTS.md — if the grep is not clean, the claim cannot be recorded"
  prohibitions:
    - statement: "MUST NOT record DOM-02 or DOM-04 as delivered/complete/met — they are deliberately descoped per D-02..D-06 and MUST be labeled descoped, with the accepted consequences (packetory.vercel.app stays live as a permanent duplicate-content URL; www.packetory.dev keeps failing TLS) preserved in the docs, not silently dropped or reframed as done"
      category: transparency
      status: unverified
      flagged: true
  assumptions:
    - "[probe DOM-01 / unclassified] 'unclassified — review manually' -> RESOLVED via D-01: DOM-01 verified live this session (curl -sI -> HTTP/2 200, valid TLS CN=packetory.dev issued 2026-07-25). Treated as an already-met criterion, not re-litigated as unresolved."
    - "[probe DOM-02 / unclassified] 'unclassified — review manually' -> RESOLVED via D-02/D-03/D-04: DOM-02 deliberately descoped by the user after live evidence; no redirect implemented; recorded as descoped in project docs."
    - "[probe DOM-03 / adjacency] 'When two things are exactly equal or just touch, do they merge, collide, or separate?' -> N/A for this requirement: DOM-03 is a repo-wide literal-string grep audit, not algorithmic set/interval logic. Resolved via D-08 (grep must return zero matches)."
    - "[probe DOM-03 / empty] 'What is the result for empty, single-element, or null input?' -> N/A: there is no algorithmic input; the audit is a fixed grep over a known file set (app/, vercel.json, next.config.ts). Resolved via D-08."
    - "[probe DOM-03 / ordering] 'When elements compare equal, is output order specified and stable?' -> N/A: the audit yields a boolean zero-match verdict, not an ordered output collection. Resolved via D-08."
    - "[probe DOM-04 / unclassified] 'unclassified — review manually' -> RESOLVED via D-05/D-06: DOM-04 runbook deliberately descoped; no runbook doc created and DEPLOY.md not extended; recorded as descoped."
---

<objective>
Reconcile the true, final status of Phase 7's four domain-cutover requirements in the project documentation, backed by a live confirmation audit — because the domain work is already done manually and most of the originally-planned code work was deliberately descoped during discussion.

- **DOM-01** (site live at packetory.dev over HTTPS): already verified live during discussion (D-01) — record as verified-live, no code or user action.
- **DOM-02** (redirects from packetory.vercel.app / www to apex): deliberately descoped (D-02/D-03/D-04) — record as descoped, do NOT add any redirect rule.
- **DOM-03** (all origin references use packetory.dev): confirm via a repo-wide audit grep (D-07/D-08) — zero code changes expected; record as complete with grep evidence.
- **DOM-04** (registration/DNS runbook): deliberately descoped (D-05/D-06) — record as descoped, do NOT write a runbook.

Purpose: leave zero false "Pending" status in project docs, preserve the user's explicit scope-narrowing decisions (and their accepted consequences) for future readers, and prove DOM-03 with real evidence rather than assumption.
Output: reconciled REQUIREMENTS.md, ROADMAP.md, and PROJECT.md. No application-code changes.

## Multi-Source Coverage Audit

| Source item | Type | Covered by |
|-------------|------|------------|
| GOAL: code-side cutover pieces in place / status truthfully recorded | GOAL | Task 1 (audit), Tasks 2-3 (docs) |
| DOM-01 (site live at packetory.dev over HTTPS) | REQ | Task 2 + Task 3 (record verified-live per D-01) |
| DOM-02 (redirects to apex) | REQ | Task 2 + Task 3 (record descoped per D-02/D-03/D-04) |
| DOM-03 (all origin refs use packetory.dev) | REQ | Task 1 (audit) + Task 2 (record complete per D-07/D-08) |
| DOM-04 (registration/DNS runbook) | REQ | Task 2 + Task 3 (record descoped per D-05/D-06) |
| D-01 verified-live | CONTEXT | Task 3 (PROJECT.md / ROADMAP.md) |
| D-02/D-03/D-04 redirect descope | CONTEXT | Task 2 + Task 3 |
| D-05/D-06 runbook descope | CONTEXT | Task 2 + Task 3 |
| D-07/D-08 canonical-URL confirm-not-rewrite | CONTEXT | Task 1 |
| RESEARCH | — | none exists (research skipped this run) — not a gap |

All source items COVERED. No unplanned items; no phase split needed.

## Artifacts this phase produces

Documentation-only. No new code symbols, no new files, zero application-code changes expected.

- `.planning/REQUIREMENTS.md` — modified: DOM-01/DOM-03 marked complete, DOM-02/DOM-04 marked descoped, Traceability table + Out of Scope reconciled.
- `.planning/ROADMAP.md` — modified: Phase 7 success criteria 2/3/5 marked descoped, DOM-01 recorded verified-live, phase status/plan list finalized.
- `.planning/PROJECT.md` — modified: v1.1 progress note reconciled (DOM-01 verified-live; DOM-02/DOM-04 descoped with accepted consequences).
- No files created under `app/`, `lib/`, `components/`; `next.config.ts` and `vercel.json` left unchanged (D-03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-production-domain-cutover-packetory-dev/07-CONTEXT.md
@app/sitemap.ts
@app/robots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOM-03 confirmation audit — repo-wide origin-literal grep + explicit file checks</name>
  <files>(no files modified — read-only audit; evidence recorded in the plan SUMMARY)</files>
  <read_first>
    - app/sitemap.ts (SITE_URL constant, line 9 — the single origin source of truth)
    - app/robots.ts (derives sitemap: URL from SITE_URL)
    - app/tools/uuid/page.tsx, app/tools/subnet/page.tsx, app/tools/dns/page.tsx, app/tools/mac/page.tsx (each derives CANONICAL_URL / openGraph.url from SITE_URL)
    - next.config.ts and vercel.json (confirm no redirect rule, no hardcoded origin)
    - .planning/phases/07-production-domain-cutover-packetory-dev/07-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
    Run the DOM-03 confirmation pass described in D-08. Do NOT rewrite anything — this is an audit, and per D-07 the codebase already derives every origin from the SITE_URL constant.

    1. Repo-wide grep for the legacy Vercel preview origin literal (the packetory.vercel.app host) across application code only: the app/ directory, vercel.json, and next.config.ts. Scope the grep to application code — do NOT scan the .planning/ directory (this plan and the docs it edits legitimately mention the legacy host as descope/consequence text, and are not part of the shipped app). Expected result: zero matches.
    2. Confirm no .env files exist that could hold a hardcoded origin (repo currently has none) — if any appear, grep them too.
    3. Explicitly verify the origin is not hardcoded outside SITE_URL in the places D-08 calls out: the four tool page.tsx metadata/openGraph blocks, the JSON-LD (application/ld+json) blocks in those pages, and any absolute og:image / metadataBase URL. Confirm the JSON-LD "@context": "https://schema.org" strings are the standard schema.org context IRI (NOT a site-origin reference) and therefore not a DOM-03 concern.
    4. Confirm next.config.ts and vercel.json contain no host-based redirect rule (per D-03 none should be added, and none should already exist).
    5. Capture the exact grep command(s) and their zero-match output verbatim in the SUMMARY as the DOM-03 evidence. If — contrary to expectation — any packetory.vercel.app literal is found in application code, replace it with a value derived from SITE_URL (import SITE_URL from @/app/sitemap), add that file to files_modified, and note the deviation; do NOT introduce a redirect as a fix.
  </action>
  <verify>
    <automated>bash -c '! grep -rIn "packetory.vercel.app" app vercel.json next.config.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rIn "packetory.vercel.app" app vercel.json next.config.ts` returns zero matches (command exits non-zero because nothing matched).
    - `grep -n "SITE_URL" app/sitemap.ts` shows `export const SITE_URL = "https://packetory.dev";`.
    - `grep -rIn "redirects\|redirect(" next.config.ts vercel.json` returns zero matches (D-03 — no redirect rule present or added).
    - The SUMMARY contains the verbatim audit grep command and its zero-match output as DOM-03 evidence.
    - No files under app/, lib/, components/ are modified by this task; next.config.ts and vercel.json are unchanged.
  </acceptance_criteria>
  <done>DOM-03 is confirmed by a clean repo-wide audit: zero legacy-origin literals in application code, every origin reference derives from SITE_URL, no redirect rule present, evidence captured in the SUMMARY. Zero code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile REQUIREMENTS.md — DOM-01/03 complete, DOM-02/04 descoped</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Domain / Infrastructure list lines 16-19; Out of Scope table; Traceability table lines 33-39)
    - .planning/phases/07-production-domain-cutover-packetory-dev/07-CONTEXT.md (D-01, D-04, D-06, D-08)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md to record the real final status of all four DOM requirements. Use scoped edits (do not rewrite the whole file).

    1. DOM-01 (line 16): change the checkbox to checked and append a status clause noting it is verified live — reachable at packetory.dev over valid HTTPS, confirmed 2026-07-25 via curl HTTP/2 200 with a valid TLS certificate (CN=packetory.dev), per Phase 7 D-01.
    2. DOM-03 (line 18): change the checkbox to checked and append a status clause noting it was confirmed by a repo-wide audit (zero packetory.vercel.app literals in application code; every origin reference derives from the SITE_URL constant), per Phase 7 D-07/D-08 — no code rewrite was needed.
    3. DOM-02 (line 17): mark descoped, NOT complete. Append a clause stating it was deliberately descoped in Phase 7 (D-02/D-03/D-04): the packetory.vercel.app -> apex and www.packetory.dev -> apex redirects were intentionally not implemented; accepted consequence — packetory.vercel.app remains a live duplicate-content URL and www.packetory.dev keeps failing TLS until revisited in a future phase.
    4. DOM-04 (line 19): mark descoped, NOT complete. Append a clause stating the runbook was deliberately not written (D-05/D-06) because the user already completed the one-time manual registration/Vercel-dashboard steps and does not want documentation of a completed one-time action.
    5. Update the Traceability table (lines 33-39): set DOM-01 status to Complete, DOM-03 to Complete, DOM-02 to Descoped, DOM-04 to Descoped.
    6. Update the Out of Scope table to add the two descoped items (host-based redirects; domain/DNS runbook) with the reason "deliberately descoped in Phase 7 per user decision (see PROJECT.md / Phase 7 CONTEXT D-02..D-06)", so the descope has a durable, discoverable home.
    Preserve every other requirement and table row unchanged.
  </action>
  <verify>
    <automated>bash -c 'grep -qi "descoped" .planning/REQUIREMENTS.md && grep -Eqi "DOM-02.*descoped|descoped.*DOM-02" .planning/REQUIREMENTS.md && grep -Eqi "DOM-04.*descoped|descoped.*DOM-04" .planning/REQUIREMENTS.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DOM-01" .planning/REQUIREMENTS.md` shows a checked item mentioning verified-live / HTTPS confirmed.
    - `grep -n "DOM-03" .planning/REQUIREMENTS.md` shows a checked item mentioning the audit / SITE_URL-derived origin.
    - The Traceability table shows DOM-02 = Descoped and DOM-04 = Descoped (not "Pending"), and DOM-01 = Complete, DOM-03 = Complete.
    - `grep -ci "pending" .planning/REQUIREMENTS.md` on the Traceability rows for DOM-01..DOM-04 returns 0 (no DOM requirement is left Pending).
    - The word "descoped" appears against both DOM-02 and DOM-04, and the accepted duplicate-content / www-TLS consequence text is present.
  </acceptance_criteria>
  <done>REQUIREMENTS.md truthfully records DOM-01 and DOM-03 as complete and DOM-02 and DOM-04 as descoped (with rationale and accepted consequences), Traceability table matches, and no DOM requirement remains Pending.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile ROADMAP.md and PROJECT.md — Phase 7 criteria descoped, DOM-01 verified-live</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 7 section lines 52-65: goal + 5 success criteria; Progress table row for Phase 7)
    - .planning/PROJECT.md ("Current Milestone: v1.1" lines 18-28; Context "v1.1 progress" note line 74)
    - .planning/phases/07-production-domain-cutover-packetory-dev/07-CONTEXT.md (D-01, D-04, D-06)
  </read_first>
  <action>
    Edit ROADMAP.md and PROJECT.md (scoped edits, preserve all other phases/sections) to reflect Phase 7's narrowed, now-final outcome.

    ROADMAP.md — Phase 7 section (lines 52-65):
    1. Annotate success criterion 1 (DOM-03 origin references) as met — confirmed by repo-wide audit, zero legacy-origin literals, per D-08.
    2. Annotate success criterion 4 (DOM-01 live HTTPS) as met — verified live 2026-07-25 (HTTP/2 200, valid TLS CN=packetory.dev), per D-01.
    3. Mark success criteria 2 and 5 (DOM-02 redirects) as DESCOPED — with a one-line rationale that the user deliberately chose not to implement redirects after being shown live evidence, accepting that packetory.vercel.app stays live and www.packetory.dev keeps failing TLS (D-02/D-03/D-04).
    4. Mark success criterion 3 (DOM-04 runbook) as DESCOPED — the runbook was deliberately not written; the user already completed the manual steps (D-05/D-06).
    5. Update the Progress table row for Phase 7 (line 77) to reflect the completed/reconciled status rather than "Not started".

    PROJECT.md — Context "v1.1 progress" note (line 74):
    6. Update the note so it states Phase 7 is complete: DOM-01 verified live (2026-07-25, HTTP/2 200 + valid TLS), DOM-03 confirmed by audit (origin already fully derived from SITE_URL), and DOM-02 + DOM-04 deliberately descoped per the user's explicit, live-evidence-informed decision — preserving the accepted consequences (packetory.vercel.app remains a live duplicate-content URL; www.packetory.dev fails TLS) for future readers. Keep wording/placement to your discretion per the CONTEXT "Claude's Discretion" note, as long as the rationale is preserved.
    Do NOT touch the Key Decisions table mechanics beyond optionally adding a single row if you judge it useful; the transition workflow owns the fuller PROJECT.md evolution. Do NOT add any redirect config or runbook (D-03/D-05).
  </action>
  <verify>
    <automated>bash -c 'grep -qi "descoped" .planning/ROADMAP.md && grep -qi "verified" .planning/PROJECT.md && grep -Eqi "packetory.dev" .planning/PROJECT.md'</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 7 section marks success criteria 2, 3, and 5 as descoped (the string "descoped"/"DESCOPED" appears against each) with rationale referencing the user decision.
    - ROADMAP.md Phase 7 records DOM-01 as verified-live (HTTP/2 200 + valid TLS, 2026-07-25) against success criterion 4.
    - ROADMAP.md Progress table row for Phase 7 no longer reads "Not started".
    - PROJECT.md v1.1 progress note states DOM-01 verified-live and DOM-02/DOM-04 descoped, preserving the duplicate-content / www-TLS consequences.
    - No redirect rule added to next.config.ts / vercel.json and no runbook doc created (grep for "redirects" in next.config.ts / vercel.json returns 0; no new DEPLOY-style runbook file added).
  </acceptance_criteria>
  <done>ROADMAP.md Phase 7 and PROJECT.md v1.1 progress truthfully reflect DOM-01 verified-live, DOM-03 audit-confirmed, and DOM-02/DOM-04 deliberately descoped — with rationale and accepted consequences preserved, and no redirect/runbook artifacts introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase adds no runtime code, no request-handling path, no new external call, and no new dependency. It modifies only `.planning/` markdown docs and runs one read-only audit grep. No new trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Repudiation / Tampering | Project documentation status (REQUIREMENTS/ROADMAP/PROJECT) | low | mitigate | Docs must record descoped requirements as descoped (not "done") and must preserve the accepted duplicate-content / www-TLS consequences — enforced by the transparency prohibition in must_haves and the Task 2/3 acceptance criteria. Prevents a future reader being misled into thinking DOM-02/DOM-04 shipped. |
| T-07-02 | Information Disclosure | DOM-03 audit grep | low | accept | The audit only reads source already in the repo and greps for a public hostname literal; no secret is read or emitted. No new exposure. |
| T-07-SC | Tampering | npm/pip/cargo installs (supply chain) | n/a | accept | No package installs occur in this phase (docs + grep only), so no Package Legitimacy Gate / slopcheck applies. No install task exists to gate. |

No new attack surface is introduced. The only genuine risk is documentation inaccuracy (misrepresenting a descope as a delivery), which is mitigated by the transparency prohibition and the acceptance criteria above — not fabricated to pad this section, per the security contribution's guidance for a docs-only phase.
</threat_model>

<verification>
Phase-level checks:
- `bash -c '! grep -rIn "packetory.vercel.app" app vercel.json next.config.ts'` — zero legacy-origin literals in application code (DOM-03).
- `grep -n "SITE_URL" app/sitemap.ts` — SITE_URL is `https://packetory.dev` and is the single origin source of truth.
- `grep -Eqi "DOM-02.*descoped|descoped.*DOM-02" .planning/REQUIREMENTS.md` and the same for DOM-04 — both recorded descoped, not Pending.
- ROADMAP.md Phase 7 success criteria 2/3/5 marked descoped; DOM-01 recorded verified-live.
- PROJECT.md v1.1 progress note reconciled (DOM-01 verified-live; DOM-02/DOM-04 descoped with consequences).
- `git diff --name-only` shows only `.planning/*.md` changed — no application-code files touched; `next.config.ts` and `vercel.json` unchanged (D-03); no runbook doc created (D-05).
</verification>

<success_criteria>
- DOM-03 confirmed by a clean repo-wide audit, evidence captured in the SUMMARY (zero code changes).
- DOM-01 recorded as verified-live; DOM-03 recorded as complete; DOM-02 and DOM-04 recorded as descoped (never Pending), with rationale and accepted consequences preserved across REQUIREMENTS.md, ROADMAP.md, and PROJECT.md.
- No redirect rule and no runbook document introduced (D-03/D-05 honored).
- All four requirement IDs (DOM-01..DOM-04) have a truthful, discoverable final status in project docs.
</success_criteria>

<output>
Create `.planning/phases/07-production-domain-cutover-packetory-dev/07-01-SUMMARY.md` when done. Capture the verbatim DOM-03 audit grep command and its zero-match output as the DOM-03 evidence.
</output>
