---
phase: 03-ip-subnet-calculator
plan: 05
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/tools/subnet/faq-data.ts
  - app/tools/subnet/page.tsx
  - tests/e2e/subnet-seo.spec.ts
autonomous: true
requirements: [SUBNET-04, SUBNET-05]
must_haves:
  truths:
    - "app/tools/subnet/page.tsx has a unique title, meta description, canonical URL (SITE_URL + /tools/subnet), and Open Graph tags for the Subnet tool, mirroring the UUID page's metadata block."
    - "The page includes a worked example showing a real IPv4 CIDR and a real IPv6 CIDR with their key computed fields (SUBNET-04 / SUBNET-05 outputs), and genuine FAQ content, both sourced from a single app/tools/subnet/faq-data.ts so the visible prose and the FAQPage JSON-LD cannot drift."
    - "The FAQ content explains the /31 and /127 both-addresses-usable behavior, the reverse-DNS nearest-boundary truncation note, IPv6 /48-/64 subdivision behavior, and that the CIDR is shareable via the URL."
    - "The worked-example and FAQ sections are static build-time copy with standard prose wrapping and no variable-length or user-generated input, so there is no overflow/long-text render risk (UI-SPEC static-content state)."
  artifacts:
    - "app/tools/subnet/faq-data.ts exporting faqItems (FaqItem[]), sampleIpv4, sampleIpv6, and a worked-example note"
    - "app/tools/subnet/page.tsx extended with metadata, canonical/OG, FAQPage JSON-LD, worked-example section, and FAQ section"
    - "tests/e2e/subnet-seo.spec.ts asserting title/canonical/OG + FAQ render + JSON-LD presence"
  key_links:
    - "page.tsx builds faqJsonLd by mapping over the SAME faqItems it renders on-screen (no drift), with the < -> \\u003c escape reused from the UUID page's JSON-LD block."
    - "faq-data.ts is the single source for both the visible FAQ prose and the structured-data mainEntity."
---

<objective>
Bring the Subnet tool page to full SEO/content parity with the established tool-page pattern: unique metadata + canonical/OG tags, a worked example (real IPv4 + IPv6 breakdowns), and genuine FAQ content, all from a single `faq-data.ts` that also feeds a drift-free FAQPage JSON-LD block — exactly mirroring the UUID page.

Purpose: the tool-page pattern (Phase 2 precedent) includes worked example + FAQ + structured data; this plan applies that pattern to Subnet without touching the interactive island, so it runs in parallel with the IPv4-completeness plan (03-02). The FAQ documents the phase's genuinely non-obvious behaviors (boundary prefixes, reverse-DNS truncation, subdivisions, URL sharing) so users understand the output.

Output: `app/tools/subnet/faq-data.ts`, the extended `page.tsx`, and an SEO e2e spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ip-subnet-calculator/03-CONTEXT.md
@.planning/phases/03-ip-subnet-calculator/03-UI-SPEC.md
@app/tools/uuid/page.tsx
@app/tools/uuid/faq-data.ts
@tests/e2e/uuid-seo.spec.ts
@app/sitemap.ts
@app/tools/subnet/page.tsx
</context>

## Phase Goal (MVP user story)

**As a** network engineer, **I want to** enter an IPv4 or IPv6 CIDR and instantly see its full, mathematically-correct subnet breakdown with every field copyable and the result reflected in a shareable URL, **so that** I can get my subnet answer in seconds and hand a teammate a link that reproduces it exactly.

## MVP Slice — what a user can do after this plan

Before: `/tools/subnet` computes results but has no metadata, worked example, or FAQ. After: the page is discoverable (unique title, canonical, OG, FAQPage structured data) and self-explanatory — a user (or search engine) sees a worked IPv4 + IPv6 example and FAQ answers covering the boundary prefixes, reverse-DNS truncation, subdivisions, and URL sharing.

## Artifacts this phase produces (this plan)

| Symbol / File | Kind |
|---------------|------|
| `app/tools/subnet/faq-data.ts` | new file |
| `faqItems: FaqItem[]`, `sampleIpv4`, `sampleIpv6`, worked-example note | exports |
| `app/tools/subnet/page.tsx` | modified (metadata + worked example + FAQ + JSON-LD) |
| `tests/e2e/subnet-seo.spec.ts` | new file |

<tasks>

<task type="auto">
  <name>Task 1: app/tools/subnet/faq-data.ts (single-source content)</name>
  <files>app/tools/subnet/faq-data.ts</files>
  <read_first>
    - app/tools/uuid/faq-data.ts (exact FaqItem type + faqItems + sample + note shape to mirror; doc-comment convention citing the decisions each content rule maps to)
    - .planning/phases/03-ip-subnet-calculator/03-CONTEXT.md (D-04 boundary behavior, D-05/D-06 subdivision behavior, D-01 privacy, D-02 default example)
    - .planning/phases/03-ip-subnet-calculator/03-UI-SPEC.md (boundary-case note wording; reverse-DNS non-aligned note)
  </read_first>
  <action>Create faq-data.ts mirroring the UUID faq-data.ts shape: export `type FaqItem = { question; answer }`, a `faqItems: FaqItem[]` array, `sampleIpv4` and `sampleIpv6` real example CIDRs (use the D-02 private-range `192.168.1.0/24` for IPv4 and `2001:db8::/32` for IPv6), and a one-line worked-example note. Author genuine FAQ answers covering: why /31 and /127 show both addresses usable (RFC 3021 point-to-point, no broadcast); what the reverse-DNS zone is and why a non-octet/non-nibble-aligned prefix is shown to the nearest covered boundary with a note (not a full RFC 2317 name); what the IPv6 /48-/64 subdivision suggestions do (clicking recomputes for the sub-block, D-05/D-06); and that the current CIDR is shareable via the page URL. Keep the doc-comment convention of citing the decision/requirement each content rule maps to. The FAQ may note the tool runs entirely in the browser and sends nothing you enter to a server (consistent with D-01), but it must not claim or describe collecting or reporting the entered CIDR value.</action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const f=require('child_process').execSync('npx tsx -e \"import('./app/tools/subnet/faq-data.ts').then(m=>{if(!Array.isArray(m.faqItems)||m.faqItems.length<3)process.exit(1)})\"',{stdio:'inherit'})" 2>/dev/null || npx vitest run app/tools/subnet 2>/dev/null; echo checked</automated>
  </verify>
  <acceptance_criteria>
    - faq-data.ts exports `faqItems` (>= 3 items), `sampleIpv4`, `sampleIpv6`, and a worked-example note; `npx tsc --noEmit` is clean.
    - At least one FAQ answer references the /31 or /127 both-usable behavior and at least one references URL sharing.
    - No FAQ answer claims the entered CIDR is collected, stored, or reported anywhere; any privacy statement is phrased as local/in-browser only (consistent with D-01).
  </acceptance_criteria>
  <done>A single-source faq-data.ts exists with genuine Subnet FAQ content and real IPv4/IPv6 worked-example values.</done>
</task>

<task type="auto">
  <name>Task 2: Extend page.tsx with metadata, worked example, FAQ, and FAQPage JSON-LD</name>
  <files>app/tools/subnet/page.tsx</files>
  <read_first>
    - app/tools/uuid/page.tsx (metadata block, faqJsonLd built from faqItems with the < -> < escape, worked-example + FAQ section markup and typography classes to mirror)
    - app/tools/subnet/faq-data.ts (the content source to import)
    - app/sitemap.ts (SITE_URL export for the canonical/OG URL)
    - .planning/phases/03-ip-subnet-calculator/03-UI-SPEC.md (page structure steps 6-7: worked example then FAQ; typography roles)
  </read_first>
  <action>Extend page.tsx (keeping it a static Server Component that still renders `<SubnetToolLoader />` as its only dynamic subtree and takes no request-time query prop): add a `metadata` export with a unique Subnet title, description, `alternates.canonical` = `${SITE_URL}/tools/subnet`, and Open Graph tags, mirroring UuidPage. Import `faqItems`, `sampleIpv4`, `sampleIpv6`, and the note from `./faq-data`. Build `faqJsonLd` by mapping over the SAME `faqItems` array (FAQPage / Question / acceptedAnswer), and inject it via the one `dangerouslySetInnerHTML` script with the `.replace(/</g, "\\u003c")` escape reused verbatim from UuidPage (the only dangerouslySetInnerHTML on the page, over static author-controlled content). Render a worked-example section (the IPv4 and IPv6 sample CIDRs with their key fields in mono) and a FAQ section mapping over `faqItems`, reusing UuidPage's exact section markup, testids (`subnet-worked-example`, `subnet-faq-item`), and typography classes.</action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "/tools/subnet" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and the route summary lists `/tools/subnet` as a static (prerendered) route, not dynamic.
    - `grep -c "faqJsonLd\|application/ld+json" app/tools/subnet/page.tsx` is >= 1 and the JSON-LD maps over the same `faqItems` rendered on-screen.
    - `grep -c "searchParams" app/tools/subnet/page.tsx` is 0 (still static; no request-time query prop).
    - `grep -c "canonical" app/tools/subnet/page.tsx` is >= 1 with the `/tools/subnet` canonical URL.
  </acceptance_criteria>
  <done>The Subnet page has unique metadata, canonical/OG, a worked example, FAQ content, and a drift-free FAQPage JSON-LD — matching the UUID page pattern — while staying statically prerendered.</done>
</task>

<task type="auto">
  <name>Task 3: tests/e2e/subnet-seo.spec.ts (metadata + FAQ + JSON-LD)</name>
  <files>tests/e2e/subnet-seo.spec.ts</files>
  <read_first>
    - tests/e2e/uuid-seo.spec.ts (exact title/canonical/OG/JSON-LD assertion patterns to mirror)
    - app/tools/subnet/page.tsx (the metadata/testids to assert against)
  </read_first>
  <action>Create subnet-seo.spec.ts mirroring uuid-seo.spec.ts: load `/tools/subnet` and assert the unique document title, the canonical link tag pointing at `${SITE_URL}/tools/subnet`, the Open Graph title/description/url tags, at least one `subnet-faq-item` rendered, and a parseable FAQPage JSON-LD `<script type="application/ld+json">` whose mainEntity length equals the number of on-screen FAQ items (drift guard).</action>
  <verify>
    <automated>npx playwright test tests/e2e/subnet-seo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/subnet-seo.spec.ts` passes.
    - The spec asserts the canonical URL, OG tags, FAQ render, AND the JSON-LD mainEntity count matching the visible FAQ items.
  </acceptance_criteria>
  <done>An e2e spec proves the Subnet page's SEO metadata, FAQ render, and drift-free structured data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static FAQ content -> JSON-LD script | Author-controlled build-time content injected via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / EoP | FAQPage JSON-LD `dangerouslySetInnerHTML` | high | mitigate | Source is a static, author-controlled `faqItems` constant (no user input); the `< -> <` escape reused from the UUID page neutralizes script-break injection (same posture as Phase 2's T-02-04). |
| T-03-03 | Information Disclosure | FAQ copy about analytics | medium | mitigate | FAQ content makes no tracking claim and describes no analytics; D-01 privacy posture preserved (enforced by the faq-data grep gate). |
| T-03-SC | Tampering (supply chain) | npm installs | low | accept | No new packages this phase (D-03). Nothing to verify. |
</threat_model>

<verification>
- `npm run build` succeeds; `/tools/subnet` remains a static route.
- `npx playwright test tests/e2e/subnet-seo.spec.ts` green.
- `npx tsc --noEmit && npm run lint` clean.
</verification>

<success_criteria>
The Subnet page is discoverable and self-explanatory: unique title/description/canonical/OG, a worked IPv4 + IPv6 example, genuine FAQ content, and a drift-free FAQPage JSON-LD — all from a single content source, matching the established tool-page pattern, with the route still statically prerendered.
</success_criteria>

<output>
Create `.planning/phases/03-ip-subnet-calculator/03-05-SUMMARY.md` when done.
</output>
