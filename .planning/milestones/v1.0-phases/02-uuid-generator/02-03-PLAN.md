---
phase: 02-uuid-generator
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/tools/uuid/page.tsx
  - app/tools/uuid/faq-data.ts
  - tests/e2e/uuid-seo.spec.ts
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "The page head contains exactly one canonical link, one title, and one meta description, plus Open Graph tags, with no duplicate or conflicting entries (QUAL-01)."
    - "Metadata values (title, description, canonical URL, OG tags) are static build-time constants with no user-derived or empty input path; title and description both mention UUID v4 and v7 (QUAL-01, D-12)."
    - "Each FAQ question appears exactly once with a distinct question/answer pair — no duplicate or merged entries (QUAL-02, D-11)."
    - "The page always renders non-empty worked-example prose (a real v4 and a real v7 side by side with a one-line 'when to use each' note) and 3-4 FAQ items — never a zero-content section (QUAL-02, D-09/D-11)."
    - "FAQ items render in a fixed authored order matching the FAQPage JSON-LD mainEntity order (QUAL-02)."
    - "The worked-example and FAQ sections are static build-time copy with no variable-length user input — standard prose wrap, no truncation risk (QUAL-02)."
    - statement: "Metadata and JSON-LD tag emission is deterministic across rebuilds with no duplicate title/canonical tags"
      verification: backstop
  artifacts:
    - app/tools/uuid/page.tsx
    - app/tools/uuid/faq-data.ts
    - tests/e2e/uuid-seo.spec.ts
  key_links:
    - "page.tsx metadata reuses SITE_URL from app/sitemap.ts for the canonical/OG url — no third hardcode of the origin literal."
    - "The visible FAQ prose and the FAQPage JSON-LD both derive from the same faqItems array, so structured data and on-screen content cannot drift."
  prohibitions:
    - statement: "MUST NOT assert UUIDs are guaranteed unique as absolute fact — FAQ/worked-example copy must present uniqueness as probabilistic (collision probability negligible, not impossible)."
      status: unverified
      verification: judgment
      flagged: true
    - statement: "MUST NOT overstate UUID v7 as a strict global monotonic sequence — describe it as time-ordered/sortable, not a guaranteed cross-machine global ordering."
      status: unverified
      verification: judgment
      flagged: true
---

<objective>
Give /tools/uuid its SEO/discovery layer and genuine content: unique per-page metadata (title, description, canonical, Open Graph), a worked example showing a real v4 and v7 side by side, and 3-4 practical FAQ items backed by FAQPage JSON-LD in the server-rendered HTML. This establishes the metadata + worked-example + FAQ tool-page pattern every later tool reuses.

Purpose: Deliver QUAL-01 and QUAL-02 (D-09, D-10, D-11, D-12).
Output: extended `page.tsx` (metadata + static prose + JSON-LD), a `faq-data.ts` single-source array, and a dedicated e2e SEO spec.
</objective>

## Artifacts this phase produces (created/modified in 02-03)

- `app/tools/uuid/faq-data.ts` — exports `faqItems: { question: string; answer: string }[]` (3-4 entries) and the worked-example sample values (one real v4, one real v7 string constant).
- `app/tools/uuid/page.tsx` — extended `metadata` (title, description, `alternates.canonical`, `openGraph`), worked-example section, FAQ section, and the `<script type="application/ld+json">` FAQPage block.
- `tests/e2e/uuid-seo.spec.ts` — Playwright spec asserting head metadata + FAQ content (separate file from uuid.spec.ts so this plan runs in parallel with 02-02 with no file conflict).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-uuid-generator/02-CONTEXT.md
@.planning/phases/02-uuid-generator/02-RESEARCH.md
@.planning/phases/02-uuid-generator/02-PATTERNS.md
@.planning/phases/02-uuid-generator/02-UI-SPEC.md
@.planning/phases/02-uuid-generator/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: faq-data.ts — single-source FAQ items + worked-example sample values</name>
  <files>app/tools/uuid/faq-data.ts</files>
  <read_first>
    - .planning/phases/02-uuid-generator/02-CONTEXT.md (D-09 worked example; D-10 practical/technical dev-search questions; D-11 3-4 questions)
    - .planning/phases/02-uuid-generator/02-RESEARCH.md (FAQPage JSON-LD example; the two prohibitions on uniqueness + v7 monotonicity claims)
    - lib/uuid/generate.ts (to produce real sample v4/v7 values at authoring time)
  </read_first>
  <action>
    Create `app/tools/uuid/faq-data.ts` exporting `faqItems: { question: string; answer: string }[]` with 3-4 entries reflecting D-10's practical/dev-search framing: at minimum "What's the difference between UUID v4 and v7?", "Are UUIDs guaranteed unique?", and "Can I use a UUID as a database primary key?". Author the "Are UUIDs guaranteed unique?" answer to present uniqueness as PROBABILISTIC (collision probability negligible for practical purposes, not an absolute impossibility) — this satisfies the transparency prohibition. Author the v4-vs-v7 answer to describe v7 as time-ordered / sortable (good for DB primary keys because index locality improves), WITHOUT claiming a strict global monotonic sequence across independent generators.
    Also export two constant sample strings — one real v4 and one real v7 value (paste actual outputs from generate.ts) — plus a one-line "when to use each" note (v7 = sortable/time-ordered) for the worked example (D-09).
  </action>
  <verify>
    <automated>npm run typecheck && grep -vE '^\s*//' app/tools/uuid/faq-data.ts | grep -Eiq "probab|negligible|not guaranteed|extremely unlikely" && echo "uniqueness hedged"</automated>
  </verify>
  <done>faq-data.ts exports 3-4 FAQ items with practical dev-search framing, a probabilistically-hedged uniqueness answer, a v7-sortability answer that does not overstate global monotonicity, and real v4/v7 sample constants for the worked example.</done>
  <acceptance_criteria>
    - `faqItems` has between 3 and 4 entries, each with non-empty distinct `question` and `answer`.
    - The uniqueness answer uses hedging language (probabilistic / negligible / not guaranteed), never "guaranteed unique" as an absolute.
    - The worked-example v4 sample matches `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/` and the v7 sample has "7" in the version nibble.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: page.tsx — metadata (canonical/OG), worked example, FAQ prose + FAQPage JSON-LD</name>
  <files>app/tools/uuid/page.tsx</files>
  <read_first>
    - app/tools/uuid/page.tsx (the skeleton from 02-01 — extend the metadata export and add sections around the existing UuidToolLoader)
    - app/privacy/page.tsx (Heading h2 / Body typography token classes and section markup to reuse verbatim)
    - app/sitemap.ts (SITE_URL export — import it for the canonical/OG url; note app/layout.tsx has no metadataBase, so use an absolute canonical string)
    - .planning/phases/02-uuid-generator/02-RESEARCH.md (Code Examples: canonical/OG metadata; FAQPage JSON-LD with the required "<" -> "<" escape before injection; D-12)
    - app/tools/uuid/faq-data.ts (faqItems + sample constants from Task 1)
  </read_first>
  <action>
    Extend the `metadata` export in page.tsx: keep the D-12 title "UUID Generator (v4 & v7) — Packetory"; add a `description` that mentions BOTH v4 and v7 (D-12); add `alternates: { canonical: \`${SITE_URL}/tools/uuid\` }` importing `SITE_URL` from "@/app/sitemap"; add `openGraph` with title, description, `url: \`${SITE_URL}/tools/uuid\``, and `type: "website"` (text-only OG — no image asset this phase, per RESEARCH A4).
    Add a "Worked example" section (h2 Heading token) rendering the real v4 and v7 sample values side by side in Geist Mono (Body size) with the one-line "when to use each" note (D-09). Add a "FAQ" section mapping `faqItems` to visible question (Label role) + answer (Body) pairs in authored order (D-10/D-11).
    Add the FAQPage JSON-LD: build the `{ "@context":"https://schema.org", "@type":"FAQPage", mainEntity: faqItems.map(...) }` object from the SAME faqItems array, then inject via a `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: <serialized> }} />` where the serialized string is `JSON.stringify(faqJsonLd)` with every "<" replaced by its unicode escape (the XSS mitigation from the official Next.js JSON-LD guide — T-02-04). This must be server-rendered (page.tsx is a Server Component), never client-injected.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && grep -q "SITE_URL" app/tools/uuid/page.tsx && grep -q "application/ld+json" app/tools/uuid/page.tsx && grep -q "u003c" app/tools/uuid/page.tsx</automated>
  </verify>
  <done>page.tsx exports full metadata (title+description mention both versions, canonical + OG from SITE_URL), renders a worked example (real v4 + v7 + note) and 3-4 FAQ items, and emits a server-rendered FAQPage JSON-LD block with the "<"-escape applied.</done>
  <acceptance_criteria>
    - `grep -q "alternates" app/tools/uuid/page.tsx` and the canonical uses `SITE_URL` (no hardcoded "https://packetory.dev" literal in page.tsx).
    - `grep -q "openGraph" app/tools/uuid/page.tsx`.
    - The JSON-LD injection escapes "<" (`grep -q "u003c" app/tools/uuid/page.tsx`) — the only dangerouslySetInnerHTML in this file.
    - The FAQ JSON-LD `mainEntity` is built from the same `faqItems` array used for the visible FAQ.
    - `npm run typecheck` and `npm run lint` exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: uuid-seo.spec.ts — metadata + FAQ content e2e (own file, no conflict with 02-02)</name>
  <files>tests/e2e/uuid-seo.spec.ts</files>
  <read_first>
    - tests/e2e/privacy.spec.ts and tests/e2e/ip-widget.spec.ts (Playwright spec shape, head/content assertions)
    - app/tools/uuid/page.tsx (rendered head + section structure)
    - .planning/phases/02-uuid-generator/02-VALIDATION.md (Test Map for QUAL-01 metadata, QUAL-02 faq)
  </read_first>
  <action>
    Create `tests/e2e/uuid-seo.spec.ts` (deliberately separate from uuid.spec.ts so this plan shares no file with 02-02 in wave 2). Add tests: "metadata is unique and complete" — `page.goto("/tools/uuid")`, assert `page.title()` contains both "v4" and "v7", assert exactly one `link[rel=canonical]` with href ending "/tools/uuid", assert one `meta[name=description]` mentioning both versions, and assert `meta[property="og:title"]`/`og:url` are present. "faq content is present and JSON-LD matches" — assert the visible FAQ shows the expected questions, assert a `script[type="application/ld+json"]` exists whose parsed JSON `@type` is "FAQPage" and whose `mainEntity` length equals the number of visible FAQ questions (proving no drift). "worked example shows a real v4 and v7" — assert both sample values are visible in the worked-example section.
    Use `page.locator('head ...')` / `page.$$eval` for head inspection; use getByTestId or role/text for content.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/uuid-seo.spec.ts</automated>
  </verify>
  <done>uuid-seo.spec.ts passes: metadata uniqueness/completeness (both versions, single canonical, OG), FAQ content presence, JSON-LD FAQPage parse + mainEntity count matching visible questions, and worked-example sample visibility.</done>
  <acceptance_criteria>
    - `tests/e2e/uuid-seo.spec.ts` exists and is the only e2e file this plan touches.
    - A test parses the ld+json script and asserts `@type === "FAQPage"` and `mainEntity.length` equals the visible FAQ count.
    - `npx playwright test tests/e2e/uuid-seo.spec.ts` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time content -> server HTML | FAQ/worked-example data is serialized into the initial HTML, including a JSON-LD `<script>` via dangerouslySetInnerHTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering (XSS) | FAQPage JSON-LD dangerouslySetInnerHTML in page.tsx | high | mitigate | Escape "<" to its unicode form in the serialized JSON-LD string before injection (official Next.js JSON-LD guide). This is the only dangerouslySetInnerHTML in the phase; the data is static/author-controlled, and the escape closes the residual script-break vector. Verified by the `u003c` grep + the e2e JSON-LD parse. |
| T-02-05 | Information Disclosure | static worked-example sample values | low | accept | The v4/v7 samples are illustrative build-time constants, never claimed as the visitor's generated value and never user-derived; no user data is server-rendered. |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test:e2e` green (uuid-seo.spec.ts included).
- Google Rich Results / manual: the FAQPage JSON-LD is present in view-source (server-rendered), parses, and its questions match the visible FAQ.
- Two judgment-tier prohibitions (uniqueness hedge, v7 monotonicity) are surfaced for human review of the FAQ copy.
</verification>

<success_criteria>
/tools/uuid has a unique title/description mentioning both versions, a canonical URL and Open Graph tags, a worked example with a real v4 and v7 side by side, and 3-4 genuine FAQ items backed by valid, non-drifting FAQPage JSON-LD — with uniqueness and v7 claims honestly hedged.
</success_criteria>

<output>
Create `.planning/phases/02-uuid-generator/02-03-SUMMARY.md` when done.
</output>
